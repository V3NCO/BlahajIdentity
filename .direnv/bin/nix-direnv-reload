#!/usr/bin/env bash
set -e
if [[ ! -d "/home/venco/WebstormProjects/BlahajIdentity" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/venco/WebstormProjects/BlahajIdentity")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/venco/WebstormProjects/BlahajIdentity" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/venco/WebstormProjects/BlahajIdentity/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/venco/WebstormProjects/BlahajIdentity/.envrc" "/home/venco/WebstormProjects/BlahajIdentity/.direnv"/*.rc
